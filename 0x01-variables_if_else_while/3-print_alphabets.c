#include <stdio.h>

/*
*generate a random number af said if negative or positive
*/
#include <stdio.h>

int main (void) 
{
	char ch;

	for(ch = 'a' ; ch <= 'z' ; ch++) {
		putchar(ch);
	}

	for(ch = 'A' ; ch <= 'Z' ; ch++) {
		putchar(ch);
       }
	return(0);
}
