#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*
*generate a random number af said if negative or positive
*/
#include <stdio.h>

int main () {
	char ch;

	for(ch = 'a' ; ch <= 'z' ; ch++) {
		putchar(ch);
	}

	for(ch = 'A' ; ch <= 'Z' ; ch++) {
		putchar(ch);
       }
	return(0);
}
