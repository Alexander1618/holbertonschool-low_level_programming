#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*
*generate a random number af said if negative or positive
*/
int main(void)
{
	int = i;
	int lowerCase = 97;
	int upperCase = 56;
	int lastCase = 52
	
		for(i = 0; i < lastCase; i++)
		{
			if (i =< 26)
				putchar(UpperCase + (i ))
}
	putchar(Cadena[n]); 
        n = n+1;
return (0);
}
