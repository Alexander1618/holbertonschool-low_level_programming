#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*
*generate a random number af said if negative or positive
*/
#include <stdio.h>

int main (void) {
	char ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{ 
		if (!(ch == 'e' || ch == 'q'))	      
		putchar(ch);
	}
   
	return(0);
}
