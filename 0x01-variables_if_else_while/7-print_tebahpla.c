#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*
*generate a random number af said if negative or positive
*/
#include <stdio.h>

int main () {
	char ch;

	for(ch = 'z' ; ch >= 'a' ; ch--) {
		putchar(ch);
	}
	putchar('\n');
	return(0);
}
