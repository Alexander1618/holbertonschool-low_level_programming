#include <stdio.h>
/*
*generate a random number af said if negative or positive
*/
int main(void)
   {
	   char hex[] = "0123456789abcdef";
	   int i;

	for(i = 0; i < 16; i++) {
		putchar(hex[i];
		putchar('\n');
	return(0);
}
