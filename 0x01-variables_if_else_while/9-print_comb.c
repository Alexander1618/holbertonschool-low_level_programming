#include <stdio.h>
/*
*generate a random number af said if negative or positive
*/
int main(void)
   {
	  
	   int number;

	   for (number = 48; number <= 57; number++)
	   {
	        putchar(number);
		if (number !=)
		{
		putchar(',');
		putchar(' ');
		}
	   }
return(0);
}
